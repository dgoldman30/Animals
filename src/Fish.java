public class Fish extends Animal{
    int speed;

    public Fish(String description, String icon, String name, int speed) {
        super(description, icon, name);
        this.speed = speed;
    }
}
